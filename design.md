# تصميم «محاضراتي» — مدير ملفات دراسي

## الرؤية

«محاضراتي» مدير ملفات دراسي عربي يعمل محلياً وباتجاه من اليمين إلى اليسار. ينشئ المستخدم مجلدات ومجلدات فرعية بلا حدود، ويختار أي نوع من الملفات من الهاتف ثم ينسخه إلى مساحة التطبيق الخاصة. تبقى الفهرسة والملفات على الجهاز، ولا يتطلب التطبيق حساباً أو خادماً.

صُممت الواجهة لهاتف رأسي بنسبة 9:16 وللاستخدام بيد واحدة. تبقى إجراءات «مجلد جديد» و«استيراد ملفات» في متناول الإبهام، وتظهر العمليات الخطرة في قائمة واضحة مع إمكان الاستعادة من سلة المهملات.

## الشاشات

| الشاشة | المحتوى | الوظائف الرئيسية |
|---|---|---|
| مكتبتي | مجلدات الجذر، مساحة التخزين، البحث ونمط العرض | إنشاء مجلد، استيراد، فرز، فتح وتحديد عناصر |
| مستعرض المجلد | مسار التنقل، مجلدات فرعية وملفات | نقل، إعادة تسمية، مفضلة، حذف، مشاركة |
| البحث | نتائج كل المجلدات | فرز حسب الاسم والتاريخ والحجم والنوع |
| سلة المهملات | عناصر محذوفة مؤقتاً | استعادة، حذف نهائي، إفراغ السلة |
| المفضلة | ملفات ومجلدات مثبتة | فتح أو إلغاء التثبيت |
| الإعدادات | القفل، المظهر، الخلفية وحجم البطاقات | PIN، البصمة، الوضع الليلي والتخصيص |
| شاشة القفل | PIN وبصمة | فتح التطبيق بأمان |

## التدفقات الأساسية

### بنية مجلدات غير محدودة

من «مكتبتي» ينشئ المستخدم مجلداً ويحدد اسمه ولونه وأيقونته وحجم عرضه البصري. داخل أي مجلد يمكن إنشاء مجلدات فرعية بلا حد.

### استيراد ونسخ الملفات

من داخل المجلد يفتح المستخدم منتقي ملفات أندرويد ويحدد ملفاً واحداً أو عدة ملفات من أي نوع. تُنسخ الملفات إلى التخزين الداخلي للتطبيق، ثم يحفظ فهرسها المحلي بالاسم والنوع والحجم والتاريخ والمسار.

### تحديد وإدارة عدة عناصر

يبدأ الضغط المطول حالة التحديد. يظهر شريط إجراءات يتيح «تحديد الكل»، النقل، المشاركة، المفضلة والحذف. ينقل الحذف العناصر إلى السلة قبل الإزالة النهائية.

### الأمان والمظهر

يمكن تفعيل PIN من أربعة إلى ستة أرقام مع بصمة أو تعرّف الوجه كبديل. تحفظ بيانات PIN في التخزين المشفّر، بينما تحفظ خيارات المظهر في التخزين المحلي. يدعم التطبيق الوضع الليلي وخلفية وتغيير حجم بطاقات المجلدات إلى صغير أو متوسط أو كبير.

## نموذج البيانات

| الكيان | الحقول المهمة |
|---|---|
| Folder | `id`, `name`, `parentId`, `color`, `icon`, `viewSize`, `createdAt`, `isFavorite`, `trashedAt` |
| StoredFile | `id`, `name`, `folderId`, `uri`, `mimeType`, `extension`, `size`, `createdAt`, `isFavorite`, `trashedAt` |
| Preferences | `theme`, `background`, `folderView`, `sortBy`, `lockEnabled`, `biometricEnabled` |

## الهوية البصرية

| العنصر | فاتح | داكن |
|---|---|---|
| الخلفية | `#F7F8FA` | `#101923` |
| سطح البطاقات | `#FFFFFF` | `#182431` |
| النص | `#14212B` | `#F0F5F8` |
| اللون التفاعلي | `#2C8FE8` | `#69B7FF` |
| النجاح | `#2FA56C` | `#52C987` |

«حجم المجلد» يعني حجم بطاقته المرئية في المكتبة، وليس سعة التخزين. يعرض حجم الملفات الفعلي منفصلاً داخل تفاصيلها.
